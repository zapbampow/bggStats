export type SelectionType = {
  value: string;
  label: string;
};

export type FilterButtonData = {
  filterId: number;
  label: string;
  value: string | string[];
  // component: React.Component;
};
